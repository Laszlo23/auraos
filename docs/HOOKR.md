# Hookr.fun — readable Uniswap v4 hooks

Infrastructure partner on Robinhood Chain `4663`. **Rules before you sign** — not an airdrop portal, not a Culture Coin sequel.

| Field | Value |
| --- | --- |
| Site | https://hookr.fun/ |
| X | [@hookrfun](https://x.com/hookrfun) only |
| Chain | Robinhood Chain `4663` |
| $HOOKR (verify on Hookr) | `0x18E674231A58c239Dc7DaeDcffE15Ec3A24cff5c` |
| SSOT | `src/lib/hookr.ts` |

## What Hookr is

Modular Uniswap v4 hook launchpad: compose up to five rules fixed at pool open — Anti-Snipe, Surge Fees, Auto Burn, LP Rewards, Nth-buy Pot. Agent-readable manifests / `SKILL.md` exist for integrators.

Hookr’s own security line (we mirror it): no airdrop/claim portal; Hookr does not initiate support DMs; a real contract address does not make a random URL legitimate.

## Aura fit (phased)

| Phase | What we ship now | Later |
| --- | --- | --- |
| B (this sprint) | Playbook + builders/token links + covenant language | — |
| C | Optional creator hooked pools / transparent treasury LP | Agent desk Hookr discovery |
| **4** | — | **wAURA wrapper** on RH after Base liquidity gate — [`AURA_RH_WRAPPER.md`](./AURA_RH_WRAPPER.md) · [`AURA_BASE_LIQUIDITY_PLAN.md`](./AURA_BASE_LIQUIDITY_PLAN.md) |

Aura remains the company desk (creator mints, USDG, OKX DEX). Hookr remains pool **rules**. We do **not** launch a speculative Aura/Culture meme on Hookr in this sprint. Official AURA CA stays on Base.

## Surfaces

- NFT desk playbook strategy `hookr-readable-hooks`
- `/for/builders` + token desk external link
- `/trust` + `docs/COMMUNITY_GROWTH.md` security line
- X drip: point at hookr.fun + `/trust` — never paste a surprise CA

## Hard nos

- No Hookr airdrop / claim UX inside Aura
- No surprise pool CAs — publish on `/trust` + changelog 48h ahead if we ever LP
- No Culture Coin framing
- Official links only: `hookr.fun` · `@hookrfun`
