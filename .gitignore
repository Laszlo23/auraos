# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
.wrangler/
.dev.vars

# Env secrets — never commit (keep .env.example tracked)
.env
.env.*
!.env.example

# Local overrides / IDE
*.local
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.cursor/

# Local Supabase / Docker leftovers
supabase/.temp
supabase/.branches
supabase/.env

# Build / coverage / caches
coverage
*.tsbuildinfo
.eslintcache
.cache

# Large local media mirrors (download via scripts/fetch-media.sh;
# ship share-kit clips with scripts/deploy-share-media.sh — too large for git).
# Keep posters/images tracked; video stays local + VPS-synced.
public/*.mp4
!public/.gitkeep

# OS / tooling
Thumbs.db
*.pem
*.key
secrets/
.venv-deck/
.venv-xai/

# T-0 launch treasury — never commit the key
.aura-t0-treasury.json
.aura-t0-out.json
.aura-t0-predicted.json
# Network deploy receipts (may include testnet CAs — never publish as official)
contracts/aura/Aura.sepolia.json
contracts/aura/Aura.mainnet.json
contracts/aura/Aura.*.json

# Nested leftover checkout (has its own .env — never commit)
auraos-landing-pr/

